Slotted classes now transform `functools.cached_property` decorated methods to support equivalent semantics.
A `cached_property` that conflicts with an *attrs* field or an explicit `__slots__` entry of the same name raises a `ValueError` instead of being silently dropped.
